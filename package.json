{
  "name": "motion-file-table-public",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.ts --bundle --minify --format=iife --target=es2020 --outfile=app.js",
    "test": "node tests/verify.mjs"
  },
  "dependencies": {
    "fflate": "0.8.2"
  },
  "devDependencies": {
    "esbuild": "0.25.12"
  }
}
